<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activity.MainActivity$PlaceholderFragment">

    <ImageButton
        android:id="@+id/section_btn"
        android:layout_width="@dimen/welcome_action_width"
        android:layout_height="@dimen/welcome_action_height"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/welcome_action_margin_bottom"
        android:src="@drawable/welcome_action"
        android:background="@null"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/section_label"
        android:layout_width="@dimen/welcome_action_width"
        android:layout_height="@dimen/welcome_action_height"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/welcome_action_margin_bottom"
        android:text="@string/action_now"
        android:textColor="@android:color/white"
        android:textAppearance="?android:textAppearanceLarge"
        android:gravity="center"
        android:visibility="gone"/>
</RelativeLayout>
